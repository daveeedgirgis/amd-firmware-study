services:
  - type: web
    name: amd-firmware-study
    env: static
    buildCommand: echo "No build needed - static site"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
